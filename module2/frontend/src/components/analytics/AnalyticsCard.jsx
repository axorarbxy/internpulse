export default function AnalyticsCard({ title = 'Analytics Overview' }) {
  return (
    <div className="analytics-card">
      <h4>{title}</h4>
      <p>Chart / data visualization placeholder.</p>
    </div>
  );
}
